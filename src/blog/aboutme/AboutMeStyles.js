const heroPaper = {
  p: { xs: 3, md: 5 },
  mb: 4,
  borderRadius: 2,
  backgroundColor: 'transparent',
};

const avatarBox = {
  display: 'flex',
  justifyContent: { xs: 'flex-start', md: 'center' },
  alignItems: 'center',
  height: '100%',
};

const introBox = {
  mt: 1,
  color: 'text.primary',
};

const quotePaper = {
  p: 1,
  mt: 2,
  backgroundColor: 'transparent',
  border: 'none',
};

const sectionPaper = {
  p: { xs: 2, md: 3 },
  mb: 4,
  borderRadius: 2,
  backgroundColor: 'transparent',
};

const eduImage = {
  maxWidth: '100%',
  height: 'auto',
  display: 'block',
  borderRadius: 1,
  boxShadow: 1,
};

const educationGrid = {
  display: 'flex',
  gap: 2,
};

const aboutStyles = {
  heroPaper,
  avatarBox,
  introBox,
  quotePaper,
  sectionPaper,
  eduImage,
  educationGrid,
};

export default aboutStyles;
